class Question:
    def __init__(self, q_text, q_answer):
        # what is attributes? --> self.text
        # The variables inside a class are called attributes.
        self.text = q_text
        self.answer = q_answer
